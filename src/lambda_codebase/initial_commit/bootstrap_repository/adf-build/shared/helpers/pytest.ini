# Copyright Amazon.com Inc. or its affiliates.
# SPDX-License-Identifier: MIT-0

[pytest]
testpaths = tests
# Exclude the source `terraform` helper directory (which holds runnable
# scripts, not tests) while still collecting tests under `tests/terraform`.
norecursedirs = */helpers/terraform
