# This CDK application intentionally pins an OLDER CDK library than ADF uses.
#
#   * This app  -> aws-cdk-lib==2.266.0   (pinned here)
#   * ADF itself -> aws-cdk-lib~=2.268.0  (see
#     src/lambda_codebase/initial_commit/bootstrap_repository/adf-build/shared/requirements.txt)
#
# Because the app is built inside its own virtual environment (py-cdk-venv in
# buildspec.yml), separate from ADF's environment (py-adf-venv), the two can
# use different CDK versions without any conflict. This demonstrates that you
# can manage your application's CDK version independently from ADF.
aws-cdk-lib==2.266.0
constructs>=10.0.0,<11.0.0
